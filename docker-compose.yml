services:
  bot:
    build: .
    restart: unless-stopped
    network_mode: host
    env_file: .env
    volumes:
      - ./data:/app/data
    environment:
      - TZ=${TZ:-Asia/Almaty}
      - SOCKS5_PROXY=${SOCKS5_PROXY:-}
